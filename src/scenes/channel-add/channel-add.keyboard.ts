/*
import {Markup} from 'telegraf';

export enum TEXT_BUTTONS {
    TEXT_CANCEL = '↩️ Назад',
    TEXT_NEXT = 'Зберегти ✅'
}

export const DEFAULT_ADD_CHANNEL_KEYBOARD = Markup.keyboard([
    [TEXT_BUTTONS.TEXT_CANCEL],
]).resize();

export const SAVE_ADD_CHANNEL_KEYBOARD = Markup.keyboard([
    [TEXT_BUTTONS.TEXT_CANCEL, TEXT_BUTTONS.TEXT_NEXT],
]).resize();*/
